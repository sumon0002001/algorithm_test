<div class="d-flex justify-content-center px-5 my-5 bg-info">
    <%= form_with html: {class: 'w-md-50 form d-flex flex-column form-control'} do |form| %>

    <div class="form-group mb-4">
        <%= form.text_field :full_string, :required => true, class: 'form-control', id: 'inputString', placeholder: "Longest substring" %>
    </div>

    <%= form.submit "Output", class: 'btn btn-dark  '%>

    <% end %>
</div>